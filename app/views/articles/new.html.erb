	<%= form_with model: @article , local: true ,:html => {:class => "form-horizontal center"} do |form| %>
		<% if @article.errors.any? %>
		    <div id="error_explanation">
		      <h2>
		        <%= pluralize(@article.errors.count, "error") %> prohibited
		        this article from being saved:
		      </h2>
		      <ul>
		        <% @article.errors.full_messages.each do |msg| %>
		          <li><%= msg %></li>
		        <% end %>
		      </ul>
	    	</div>
	  <% end %>
	  
	  	<div class="form-group">
		  	<p>
				<%= form.label :title , "Title:" , class:"col-md-4 control-label" %><br>
				<div class="col-md-8">
					<%= form.text_field :title%><br>
				</div>
			</p>
	  	</div>
		
		<div class="form-group">
			<p>

				<%= form.label :text , "Text:" , class:"col-md-4 control-label" %><br>
				<div class="col-md-8">
					<%= form.text_area :body %>	
				</div>
			</p>
		</div>
			
		<div class="form-group">
			<p>
				<%= form.submit "Create", class:"btn btn-default btn-primary"%>
			</p>
		</div>
		<%= link_to 'Back', articles_path %>
     <%end %>